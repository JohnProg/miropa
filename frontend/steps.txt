STEPS:

//instala nodejs (ya viene con npm)

//verifica
node --version && npm --version

//instalar modulos necesarios 
sudo npm install -g yo grunt-cli bower karma

//clonar repo
//instala el package.json
sudo npm install

//instala dependendias
bower install

//add bootsrap
bower install angular-bootstrap --save
